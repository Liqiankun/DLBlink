<!-- components/btn-like/index.wxml -->
<view class='container' bind:tap='onLike'>
  <image src="{{liked ? './images/like@2x.png' : './images/like_dis@2x.png'}}" />
  <text wx:if='{{count > 0}}'>{{count}}</text>
</view>